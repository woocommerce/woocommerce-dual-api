# Excluded from release builds (composer build:zip, wp dist-archive).
# bin/api-builder/ is deliberately NOT excluded: consumers build their
# GraphQL layer against the installed plugin.
/.ai
/.claude
/.git
/bin/build-zip.sh
/dist
/stubs
/lib/README.md
/.github
/lib/vendor
/node_modules
/tests
/tmp
/.distignore
/.gitignore
/.markdownlint.json
/docs
/.phpunit.result.cache
/composer.lock
/lib/composer.json
/lib/composer.lock
/phpcs.xml
/phpstan.neon
/phpstan-baseline.neon
/phpunit.xml
